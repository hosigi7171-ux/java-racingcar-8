# java-racingcar-precourse


> 우아한테크코스 프리코스 2주차 과제\
> 자동차 경주

---

<details>
<summary><b>요구사항 체크리스트 </b></summary>

완료시 ✅ 표시

### 과제 진행 요구 사항

* [x] 자동차 경주 게임 저장소를 **fork**하고 **clone**했다.
* [x] 기능 구현 전 `README.md`에 **구현할 기능 목록**을 정리했다.
* [ ] **기능 단위**로 커밋을 진행했다.
* [ ] 커밋 메시지는 **AngularJS Commit Message Convention**을 참고하여 작성했다.
* [ ] 프리코스 **진행 가이드 문서**를 참고하며 과제를 수행했다.

### 기능 요구 사항

* [ ] 주어진 횟수 동안 **자동차들이 전진** 또는 **멈추는 기능**을 구현했다.
* [ ] 각 **자동차 이름**을 쉼표(,)로 구분하여 입력받고, 이름은 **5자 이하**로 제한한다.
* [ ] 각 자동차는 **0부터 9 사이의 무작위 숫자**를 생성하고, **4 이상이면 전진**한다.
* [ ] 사용자가 **시도할 횟수**를 입력받으면 해당 횟수만큼 **자동차들이 전진하거나 멈추는 경주**가 진행된다.
* [ ] **전진한 자동차**는 그 이름과 함께 **`-`로 전진 거리**를 출력한다.
* [ ] **최종 우승자**를 출력하며, 우승자가 여러 명일 경우 쉼표(,)로 구분하여 출력한다.
* [ ] 사용자가 잘못된 값을 입력할 경우 **IllegalArgumentException**을 발생시키고, 애플리케이션이 정상 종료되도록 구현했다.

### 입출력 요구 사항

* [x] 입력:

    * 경주할 **자동차 이름**을 쉼표(,)로 구분하여 입력받는다.
    * 시도할 **횟수**를 입력받는다.
* [x] 출력:

    * **차수별 실행 결과**를 출력한다.

        * 예시: `pobi : ----`, `woni : ---`, `jun : ---`
    * **최종 우승자**를 출력한다.

        * 예시: `최종 우승자 : pobi`
        * 또는 **공동 우승자**가 있을 경우: `최종 우승자 : pobi, jun`

### 프로그래밍 요구 사항

* [ ] JDK 21 버전에서 실행 가능해야 한다.
* [ ] 프로그램 실행의 시작점은 **`Application`**의 `main()`이다.
* [ ] `build.gradle` 파일은 변경할 수 없으며, 제공된 라이브러리 이외의 외부 라이브러리는 사용하지 않는다.
* [ ] 프로그램 종료 시 **`System.exit()`**를 호출하지 않는다.
* [ ] 프로그래밍 요구 사항에서 달리 명시하지 않는 한 파일, 패키지 등의 이름을 바꾸거나 이동하지 않는다.
* [ ] 자바 코드 컨벤션을 지키면서 프로그래밍한다.
* [ ] 기본적으로 **Java Style Guide**를 원칙으로 한다.

### 프로그래밍 요구 사항 2

* [ ] **indent(인덴트, 들여쓰기) depth**를 3이 넘지 않도록 구현한다. 2까지만 허용한다.
  (예: while문 안에 if문이 있으면 들여쓰기는 2이다.)
* [ ] 3항 연산자를 쓰지 않는다.
* [ ] 함수(또는 메서드)가 한 가지 일만 하도록 최대한 작게 만든다.
* [ ] JUnit 5와 **AssertJ**를 이용하여 정리한 기능 목록이 정상적으로 작동하는지 테스트 코드로 확인한다.
* [ ] 테스트 도구 사용법이 익숙하지 않다면 아래 문서를 참고하여 학습한 후 테스트를 구현한다.

    * [JUnit 5 User Guide](https://junit.org/junit5/docs/current/user-guide/)
    * [AssertJ User Guide](https://assertj.github.io/doc/)
    * [AssertJ Exception Assertions](https://assertj.github.io/doc/#assertj-core-exceptions)
    * [Guide to JUnit 5 Parameterized Tests](https://www.baeldung.com/guide-to-junit-5-parameterized-tests)

</details>

---

<details>  
<summary><b>느낀점 및 고민했던 부분</b></summary>
</details>

---

## 기능 설계서

1. **RacingCar**: 자동차 객체를 관리한다. 이름과 전진 거리를 저장한다.
2. **RacingGame**: 전체 게임 흐름을 관리한다.

    * **InputReader**: 사용자로부터 자동차 이름과 시도 횟수를 입력받는다.
    * **InputValidator**: 입력값의 형식과 유효성을 검증한다.
    * **RaceRoundManager**: 경주를 차수별로 진행하며, 각 차수마다 자동차가 전진하는지 결정한다.
    * **RaceDisplay**: 경주 현황을 화면에 출력한다.
    * **ResultWriter**: 최종 우승자를 출력한다.

| 단계  | 기능 설명                             | 클래스명                   |
| --- | --------------------------------- | ---------------------- |
| 1   | 자동차 이름을 입력받는다                     | **`InputReader`**      |
| 2   | 이름의 유효성을 검증한다                     | **`InputValidator`**   |
| 3   | 시도 횟수를 입력받는다                      | **`InputReader`**      |
| 4   | 각 차수마다 자동차 리스트를 순회하며 경주를 진행한다     | **`RaceRoundManager`** |
| 4-1 | 랜덤한 숫자를 생성하고, 4 이상이면 전진한다         | **`RaceRoundManager`** |
| 4-2 | 전진 거리가 더 멀리 나갔다면 최대 거리와 우승자를 갱신한다 | **`RaceRoundManager`** |
| 4-3 | 경주 현황을 출력한다                       | **`RaceDisplay`**      |
| 5   | 최종 우승자를 출력한다                      | **`ResultWriter`**     |

---
